# gems-management-react

- Clone the repo
- cd into the folder
- `npm install`
- change XXXXXXXXXXXX in index.js to your token (staging)
- `npm start`
- open `http://localhost:8080/` in your browser


